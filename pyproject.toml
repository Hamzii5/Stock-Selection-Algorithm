[project]
name = "stock-selection-algorithm"
version = "0.1.0"
description = "Flask stock selection dashboard"
requires-python = ">=3.12"
dependencies = [
    "Flask>=3.0,<4.0"
]

[tool.vercel]
entrypoint = "app:app"
